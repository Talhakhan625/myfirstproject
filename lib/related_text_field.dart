import 'package:flutter/material.dart';

class RelatedTextField extends StatelessWidget {
  const RelatedTextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
